# Web deployment dependencies — inference subset of requirements.txt plus the
# FastAPI server. Deliberately omits matplotlib / datasets / anthropic / tqdm /
# pandas: those are only used by train.py, evaluate.py and data_collection.py,
# none of which the web server imports.

# --- Inference stack (must match versions used to train the model) ---
transformers>=4.40.0
torch>=2.0.0
sentence-transformers>=2.6.0
scikit-learn==1.6.1  # pinned: must match the version that pickled models/classifiers.joblib
sentencepiece>=0.2.0
protobuf>=4.21.0
numpy>=1.24.0
scipy>=1.10.0
joblib>=1.3.0

# --- Web server ---
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.0.0
